#include "main.h"
/**
 *_isalpha - checks for lowercase and uppercase character
 * @c: ASCII code
 * Return: 1
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
